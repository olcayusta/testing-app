describe('dummy component', () => {
  it('should lets check amount', function () {
    expect(100).toBe(100)
  });
  it('should lets check char', function () {
    expect('hello').toBe('hello')
  });
})
